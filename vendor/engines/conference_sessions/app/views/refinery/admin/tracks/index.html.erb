<div id='records'>
  <% if searching? %>
    <h2><%= t('results_for', :scope => 'shared.admin.search', :query => params[:search]) %></h2>
  <% end %>
  <% if @tracks.any? %>
    <div class='pagination_container'>
      <%= render 'tracks', :tracks => @tracks %>
    </div>
  <% else %>
    <p>
      <% unless searching? %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% else %>
        <%= t('no_results', :scope => 'shared.admin.search') %>
      <% end %>
    </p>
  <% end %>
</div>
<div id='actions'>
  <ul>
    <% if Refinery::Admin::TracksController.searchable? %>
      <li>
        <%= render "/refinery/admin/search", :url => refinery.admin_tracks_url %>
      </li>
    <% end %>
    <li>
      <%= link_to t('.create_new'), refinery.new_admin_track_url, :class => "add_icon" %>
    </li>
    <li>
      <%= link_to t('.back_to_conf_sessions'), refinery.admin_conference_sessions_url, :class => "back_icon" %>
    </li>
  </ul>
</div>


