<%= form_tag url_for(:action => :add_comment), :remote => true do %>
<%= text_area_tag(:comment, '', :id => 'comment-field', :class => 'field') %>
<%= hidden_field_tag(:proposal_id,  @proposal.id ) %>
<%= submit_tag t('.comment_button') -%>
<% end %>

