<%= form_tag(:action => :add_comment, :method => :post) do %>
<%= text_area_tag(:comment, '', :class => 'field') %>
<%= hidden_field_tag(:proposal_id,  @proposal.id ) %>
<%= submit_tag t('.comment_button') -%>
<% end%>

